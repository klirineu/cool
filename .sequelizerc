const path = require("path")

module.exports = {
  config: path.resolve(__dirname, "src", "Sequelize", "config", "database.js"),
  "migrations-path": path.resolve(__dirname, "src", "Sequelize", "database", "migrations")
}